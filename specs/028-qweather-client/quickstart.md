# SDD-028 validation

1. 沿用已有 Ed25519 私钥；根据 README 配置服务端 QWEATHER_*，不使用 NEXT_PUBLIC、不打印 JWT。
2. npm run check、npm run build、npm run verify:sdd-028；回归场景/雨雪/日期/城市门禁。
3. node scripts/verify-qweather-connection.mjs 验证真实 current、geo、daily；武汉仅为固定样本。
4. 隔离体验账号手动选择武汉/上海：生成前显示天气；切换今日/明日；网络直接请求和风，客户端无私钥。
5. 390px 验证加载、失败重试、快速切换、五分钟失效、空城市。接口未登录 401、跨源 403、非法 day 400；旧结果标为生成时快照。
6. 国内手机无代理/有代理及真实定位由用户集中验证，不用桌面成功替代。

## Evidence

2026-09-06 Production 补验：五项天气配置经 stdin 写入 Secret，部署 `dpl_95cQdxJFKMcr2SVMF8QmDJiuzEqj`（源提交 e369ea8）Ready，固定域名生效。隔离体验身份手动上海，今日28°C/体感29°C（17:54），明日09-07预报24～30°C（17:56）；浏览器直连 current/daily 路径成功，390px 无溢出、无运行错误。应用侧短 JWT 签发与服务器城市解析均通过；未修改供应商控制台应用限制，手机实际网络/定位与故障场景仍待本人验收。

2026-09-06 本地 MVP 通过，未部署。check/build、028 固定样本与短令牌签名、007/012/013/014/015/017/018/019/020 回归通过。

实现提交：`171341b`；提交前 hook 再次通过，真实私钥内容未进入暂存文件或客户端构建。开发配置只在 Git 忽略文件内。

- 独立体验浏览器 390px：未选城市不默认，今日武汉32°C/体感33°C（16:29）；明日09-07武汉21～32°C；上海24～30°C（16:36）。
- performance resource 证据：current/30.58/114.30 155ms；daily/30.58/114.30 90ms；daily/31.23/121.47 84ms。请求直达和风，Authorization 跨域请求成功。只为单次样本。
- 未登录401、跨源403、非法day400。拦截和风请求模拟故障后禁用生成；解除后恢复。真实等待超过5分钟显示 stale 提示并禁用。
- 浏览器修改隐藏 expectedWeatherDate=2000-01-01 后提交，服务端拒绝，未调用AI。012测试双账号武汉/上海相互拒绝；013真实天气与数据库日期隔离通过。
- 正常视图无 JS error、无错误覆盖层、无横向溢出。截图已视觉复核（浏览器临时目录，不作为永久交付链接）。
- 无新增数据表或权限；不操作真实账号衣橱；不调用图像模型。保留一个空衣橱测试体验身份，012/013业务测试数据脚本已清理。
- 未验：国内手机开关代理、真实设备权限与定位准确性、Production环境/凭据限制、生产供应商限流故障。缓存/限频为进程级；不能以本地成功代替上述验收。
