# 验收

1. `npm run check`、`npm run build`、`npm run verify:sdd-030`。
2. 390px 隔离体验身份检查五项底栏与三个顶部入口；浅雾丁香渐变、圆润居中标题、克制紫色 hover/active、键盘焦点和减少动态。普通底栏 item 必须保持透明，只显示浅紫气泡；中央按钮单独变色。无溢出、控制台错误。
3. 个人页保留昵称头像编辑及三项统计，无画布；推荐保留原图、换件、收藏、日记。
4. 旧画布跳转、抠图 410、确认无后台抠图，历史资产不删除。
5. 用户在百炼北京控制台获取 API Key 与 API Host，填入忽略的 `.env.local` 后重启。禁止聊天传密钥。用隔离衣橱验收三套真实 AI 结果及模型来源；未配置必须保持待办。
6. 本阶段不自动部署；发布需单独验收目标项目与凭据。

## 2026-09-06 验收记录

实现提交 `a883763`；浅紫视觉与 Dock 状态修复提交 `892b9c9`。check/build、030 固定接口与业务复验、007/014/015/017/024/025/026/028/029、027 离线回归通过。浏览器 320/390/1280px 无溢出，减少动态为 0s；导航可达、合成昵称保存成功、旧链接跳转与抠图 410/no-store 已验证，页面运行错误为空。随后按用户反馈完成浅紫校正：390px 背景为 `#eee6f5 → #f5eff9 → #fcfafe`，Dock 暖白玻璃，普通导航 item 透明、浅紫气泡显现，中央 56px 按钮 hover 为 `rgb(157,129,184)`，无整块深紫闪色。真实千问 Key/Host 未配置，T013 保持未完成，不将模拟结果记为真实模型验收。无 Production 发布。
