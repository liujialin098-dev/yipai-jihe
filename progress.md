# 衣拍即合开发进度

> 版本：MVP 分阶段开发规划 v3 ｜ 更新日期：2026-08-26
>
> 本文档依据 `D:\产品经理\衣拍即合_详细版PRD.docx` 的最新内容制定，并覆盖此前所有开发路线图。PRD 负责描述产品需求；本文档是当前唯一有效的开发顺序、阶段边界、验收与上线进度 TODO LIST。
>
> 每次 AI Agent Coding 开始前必须阅读当前阶段；每个阶段开发、验收和提交完成后，必须立即更新本文档，再开始下一阶段。

## 本次覆盖的范围变化

- [x] P0 保留匿名体验；邮箱绑定、邮件验证、登录和退出暂缓并移至 P1，不阻塞衣橱核心闭环。
- [x] P0 使用衣物原图缩略图展示搭配，不执行自动抠图。
- [x] 自动抠图移动到 P1，届时通过服务端 `cutoutService` 调用外部抠图 API。
- [x] 穿搭日记和穿着统计移动到 P1，不创建 P0 页面或写入流程。
- [x] P0 核心闭环调整为：入库 → 衣橱 → 推荐 → 换一件 → 收藏反馈。

## AI Agent Coding 估算口径

- P0 共 6 个独立 SDD，预计需要 6 段主开发对话；保留 SDD-002 编号但不计入 P0。
- 每段主对话包含：读取 SDD、实现、静态检查、独立验收、更新本文档和提交。
- S、M、L 只表示单轮 AI Coding 的上下文和任务复杂度，不代表真实生产工期。
- 遇到外部服务密钥、邮件验证或部署权限时，可以增加一轮短对话完成配置和验证，但不得扩大阶段功能范围。

## 统一进度规则

- [x] 开始阶段前，创建并确认对应 SDD 的 `spec.md`、`plan.md` 和 `tasks.md`。
- [x] 涉及数据结构时补充 `data-model.md`，涉及接口时补充 `contracts/`。
- [x] 阶段完成前补充 `quickstart.md`，写清独立验收步骤和预期结果。
- [x] 开发过程中只实现当前阶段范围，不提前混入后续功能。
- [x] 阶段完成前运行 `npm run check`，并完成该阶段全部独立验收项。
- [x] 阶段完成后更新状态、完成日期、验收结果、已知限制、下一步和提交记录。
- [x] 未通过静态检查或独立验收时，不得标记为“已完成”，也不得开始依赖该阶段的后续阶段。

## 当前总览

- 项目：衣拍即合（AI 穿搭助手）
- 当前状态：P0 的 SDD-001、SDD-003～SDD-007、全局动效 SDD-011、账号个性化 SDD-012 与真实两日天气 SDD-013 已完成；SDD-014 场景差异化推荐已完成代码和全部自动验收，等待发布到既有 `yipai-jihe` Production。SDD-002 已改为无需邮件验证的直接注册/登录，代码与 Supabase 认证配置均已完成，等待历史账号本地设密与真实账号重登录验收
- P0 目标：发布可访问、可复现的受控评审版
- 技术基线：Next.js 16.3.1、React 19、TypeScript、Tailwind CSS 4、shadcn/ui、Supabase
- Supabase 项目：`next-app-supabase`（project ref：`gmjtzmxuveoaqcdmuifr`）
- 部署基线：Vercel + 当前 Supabase 项目
- AI 基线：一个真实多模态模型服务，服务端配置，异常时规则降级
- 测试数据：由 AI Coding 创建 20～30 件演示衣物、10 件识别测试样本和多个隔离测试会话

## P0 SDD 阶段 TODO

### SDD-001：应用框架与匿名数据底座

- 状态：已完成
- SDD 目录：`specs/001-app-foundation/`
- AI Coding 估算：1 段主对话，复杂度 M
- 目标：建立移动端应用框架、匿名会话和后续模块共用的数据安全底座。

功能范围：

- [x] 建立首页、衣橱、添加衣物、推荐、收藏和设置路由骨架。
- [x] 建立移动端优先的应用布局、顶部状态区和底部导航。
- [x] 实现首次访问自动创建 Supabase 匿名用户及会话保持流程；待项目开关开启后验收。
- [x] 实现用户资料和默认偏好幂等初始化流程；待匿名会话开启后验收。
- [x] 业务数据统一绑定 Supabase Auth 用户 ID。
- [x] 建立数据库迁移目录、基础 RLS 模式和 Storage 私有路径规则。
- [x] 建立通用加载、错误、空状态和环境变量检查。
- [x] 部署第一个 Vercel Preview，并确认 Vercel 到 Supabase 的公开环境配置可用。

明确不做：

- [x] 本阶段不实现邮箱绑定。
- [x] 本阶段不实现衣橱业务功能。
- [x] 本阶段不调用 AI 服务。

独立验收：

- [x] 新浏览器可以自动获得匿名会话并进入空衣橱引导。
- [x] 刷新页面后仍是同一匿名用户。
- [x] 两个匿名会话无法读取或修改对方的资料和测试数据。
- [x] 主要路由和 Preview 环境可以正常访问。
- [x] `npm run check` 通过。

阶段完成记录：

- 完成日期：2026-08-21
- 验收结果：全部通过。390px 本地新会话 `CCF08712` 自动进入空衣橱并连续刷新 3 次身份不变；双会话 `17A89388`、`8F841A20` 的自身访问成功，交叉读取、修改和跨用户 Storage 写入全部被拦截；Preview 会话 `43FC08A9` 初始化成功，六个路由均为 200 且 3 次刷新身份一致。`npm run check`、`npm run build` 通过，两张表 RLS、最小授权、私有 bucket 和策略均已远端复核。
- 已知限制：Vercel Preview 受团队登录保护；当前部署中的两个 `NEXT_PUBLIC_` 值有效，但后续自动部署前仍需在 Vercel Preview 环境持久配置。当前只支持匿名身份，用户清除站点数据或更换设备后暂时无法恢复原身份和数据。Supabase 安全顾问有 3 项预期匿名访问策略提醒，策略实际均以当前用户 ID 或私有路径限制；另 1 项泄露密码保护提示将在未来恢复 SDD-002 时处理。
- 下一步：先持久配置 Vercel Preview 的两个 Supabase 公开变量，再按 Spec Kit 创建并确认 SDD-003 衣橱核心规格；邮箱绑定不作为 SDD-003 的依赖。
- Preview：`https://ai-coding-2m4gteem7-jialin-d583.vercel.app`（READY，受 Vercel Authentication 保护）
- 提交记录：`da86c2e`（主体实现）、`908f691`（进行中检查点文档）；SDD-001 完成状态见本次 Git HEAD 提交。

### SDD-003：我的衣橱与演示数据

- 状态：已完成
- SDD 目录：`specs/003-wardrobe-core/`
- AI Coding 估算：1 段主对话，复杂度 M
- 依赖：SDD-001
- 目标：先建立不依赖 AI 的衣橱管理能力和稳定演示数据。

功能范围：

- [x] 建立衣物单品数据结构和用户归属策略。
- [x] 实现衣橱列表、分类浏览、搜索和多条件筛选。
- [x] 实现单品详情、编辑、归档和删除。
- [x] 单品详情展示基础属性、收藏状态和推荐使用情况。
- [x] 创建 20～30 件包含类别、颜色、材质、风格、季节和场合的测试衣物。
- [x] 为测试衣物准备安全的演示原图，不使用真实个人敏感照片。
- [x] 提供当前用户“一键加载演示衣橱”入口，并保证重复执行幂等。
- [x] 建立衣物原图 Storage 权限和缩略图展示。

明确不做：

- [x] 不统计穿着次数。
- [x] 不实现穿搭日记。
- [x] 不依赖透明底抠图。

独立验收：

- [x] 用户可以查看、搜索、筛选、编辑、归档和删除演示单品。
- [x] 一键加载后得到 20 件以上可用于推荐的衣物。
- [x] 重复加载不会产生无法解释的重复数据。
- [x] 两个用户无法访问对方的衣物记录和原图。
- [x] `npm run check` 通过。

阶段完成记录：

- 完成日期：2026-08-21
- 验收结果：全部通过。新匿名会话一次得到 24 件合成衣物，连续补齐 3 次仍为 24；关键词、分类、三维组合、空结果、清除条件、有效与无效编辑、归档、恢复、取消删除、确认删除和已删除详情均正确。删除后补齐只新增 1 件并恢复到 24；390px 无水平溢出且 24 张图片无损坏。真实交互预热后 3 次采样中，关键词与三维组合筛选中位响应分别为 1.707 秒和 1.847 秒。双匿名会话记录与图片隔离、远端 RLS/授权/私有 bucket 复核、`npm run check`、`npm run build` 和 `npm run verify:sdd-003` 均通过。
- 测试数据规模：当前浏览器匿名会话 24 件安全演示衣物；棚拍素材增补后的最终隔离脚本使用 A `BE081706`、B `35DFDAD5` 并自动清理其测试记录和私有测试图片；用户确认删除的验收记录及图片不可恢复，补齐后生成了新的默认演示记录。
- 已知限制：匿名身份清除站点数据或换设备后无法恢复；首次跨区域远端请求偶有 2～4 秒波动，预热后的典型筛选满足 2 秒目标；Security Advisors 的匿名策略提醒符合当前 P0 设计，密码保护留待 SDD-002；两个新 GIN 索引因刚创建尚无累计使用统计。
- 下一步：启动 SDD-004，先确认真实多模态 AI 服务、模型名和服务端 API Key，再按 Spec Kit 完成规格澄清、计划与任务拆分。
- 提交记录：`ef5e67a`（进行中检查点）、`a8a2e46`（SDD-003 阶段完成）；本条记录由后续文档提交补充。
- 视觉增补（2026-08-21）：依据用户提供的移动端穿搭应用参考图完成全局 UI 重设计。保留原路由、文案职责和数据操作，统一为冷白银灰背景、近黑主色、系统蓝焦点色、24px 级软圆角和固定玻璃 Dock；玻璃材质为 Web 端近似实现。390px 下首页、衣橱、筛选、详情、编辑、添加、推荐、收藏和设置均无水平溢出，图片无损坏，浏览器控制台无错误。
- 素材增补（2026-08-21）：24 件演示衣物由几何示意图升级为统一的无人物电商棚拍实物图，保留原名称、分类、颜色和材质；素材压缩为 768px WebP 并作为公开演示资源随应用发布。真实用户原图仍走 Supabase 私有 Storage，不受本次变更影响。

### SDD-004：原图上传与 AI 识别入库

- 状态：已完成
- AI 服务决策：OpenAI Responses API，默认模型 `gpt-4o-mini`，可通过服务端 `OPENAI_VISION_MODEL` 覆盖；`OPENAI_API_KEY` 已配置在本地与 Vercel Preview 服务端环境。
- 临时图片决策：当前用户私有临时区保留不超过 24 小时，确认、取消或到期后清理。
- SDD 目录：`specs/004-ai-item-ingestion/`
- AI Coding 估算：1 段主对话，复杂度 L
- 依赖：SDD-003
- 目标：打通“拍照或选图 → 真实 AI 识别 → 人工修正 → 原图入库”。

功能范围：

- [x] 支持移动端拍照和相册选择 jpg/png 图片。
- [x] 校验图片格式和 10MB 大小限制。
- [x] 将原图写入当前用户的 Supabase Storage 私有路径。
- [x] 服务端通过 OpenAI Responses API 调用真实多模态模型，密钥缺失时返回稳定降级状态。
- [x] 识别类别、颜色、材质、风格、季节和适用场合。
- [x] 对 AI 结构化输出执行 JSON Schema 与服务端枚举双重校验。
- [x] 展示识别中状态、可编辑结果卡片和确认入库操作。
- [x] 支持最多 10 张的批量选择、最多 3 项并发识别、逐件修正和批量确认。
- [x] 识别失败允许重试，并保留手动修正或录入路径。
- [x] 创建 10 件固定识别测试样本，记录准确率和修正次数。

明确不做：

- [x] 不调用外部抠图 API。
- [x] 不生成透明底图。
- [x] 不将模型密钥写入浏览器代码或仓库。

独立验收：

- [x] 10 件测试样本识别准确率达到 80%，或记录未达标字段及修正结果。
- [x] 原图可上传、预览并只归属当前用户。
- [x] 用户可以修正识别结果并确认入库。
- [x] 真实 AI 正常流程可用，失败时仍可完成手动入库。
- [x] 单次识别目标响应时间不超过 10 秒，超时有明确提示。
- [x] `npm run check` 通过。

阶段完成记录：

- 完成日期：2026-08-23（功能实现检查点：2026-08-22）
- AI 服务与模型：OpenAI Responses API，`gpt-4o-mini`，`detail: low`，`store: false`，严格 JSON Schema，12 秒服务端超时；本地与 Vercel Preview 均已配置服务端 Key。
- 识别测试结果：10 张无人物逼真 jpg 固定样本类别正确 10/10（100%），类别修正 0；平均耗时 3504ms，最慢 5317ms，全部低于 10 秒目标。逐项结果见 `specs/004-ai-item-ingestion/quickstart.md`。
- 验收结果：真实批量流程完成 10 项上传、识别和确认，10 个不同项目均成功入库；OpenAI 严格 Schema 已移除不支持的 `uniqueItems`，额度不足会显示准确提示。双匿名会话 RLS/Storage 隔离、连续确认 3 次只产生 1 条记录、取消与过期清理、10 项上限、3 项并发和批量确认均通过；390px 布局沿用本阶段已通过的无水平溢出结果。`npm run check`、`npm run build` 和增强版 `npm run verify:sdd-004` 全部通过。
- 已知限制：物理过期清理由当前用户下次进入入库流程时触发，P0 未配置定时 Cron；匿名身份清除站点数据或换设备后仍无法恢复。2026-08-25 已修复 Windows 本地 Node.js 无法直连 OpenAI 的环境问题，详见下方回归记录。
- 2026-08-25 回归修复：确认密钥和额度正常、标准 Responses API 请求格式正确，故障根因是 Windows 本机仅 Node.js 网络栈连接 OpenAI 超时。新增共享服务端传输层：Windows 本地通过 PowerShell 系统网络栈发送 Base64 编码 JSON，Vercel/非 Windows 仍使用标准 `fetch`；密钥只放入子进程环境，不进入命令行、浏览器或日志。当前添加页原有 10 张图片重新识别全部成功，服务端请求均返回 200，单张约 6.4～11.6 秒。
- 下一步：启动 SDD-005，先明确天气数据方案、每日推荐输入和失败降级边界，再创建对应 Spec Kit SDD。
- Preview：`https://ai-coding-d8qt4h6g2-jialin-d583.vercel.app`（READY，包含最终 Schema 修复和额度提示）；真实 10 项 AI 批量验收在同配置的 `https://ai-coding-p2br60ssy-jialin-d583.vercel.app` 完成。
- 提交记录：SDD-004 暂停检查点 `54b6a82`；阶段完成提交 `66dbf01`（AI Schema 修复、真实验收、验证脚本与完成文档）。

### SDD-005：每日 3 套 AI 推荐

- 状态：已完成
- SDD 目录：`specs/005-daily-recommendations/`
- AI Coding 估算：1 段主对话，复杂度 L
- 依赖：SDD-003、SDD-004
- 目标：跑通产品核心价值“根据已有衣橱解决今天穿什么”。

功能范围：

- [x] 接入固定演示城市的可配置天气服务。
- [x] 天气服务缺失或失败时使用明确标识的模拟天气。
- [x] 支持通勤、休闲、约会和正式场合切换。
- [x] 将衣橱摘要、天气、场合和偏好权重提交给服务端推荐服务。
- [x] 真实 AI 返回 3 套包含真实单品 ID、搭配理由和风格标签的结构化方案。
- [x] 校验衣橱归属、单品类别、季节、体感温度和同日跨套不重复规则。
- [x] 使用原图缩略图按版位展示搭配卡，不依赖透明底图。
- [x] 同一用户同一天只保留一个推荐批次，手动刷新覆盖当前批次。
- [x] AI 失败、超时或输出无效时使用规则模板生成降级方案。
- [x] 推荐生成目标响应时间不超过 15 秒，页面显示加载和失败状态。

独立验收：

- [x] 用户每天能看到恰好 3 套逻辑通顺的方案。
- [x] 推荐只引用当前用户衣橱中的真实单品。
- [x] 切换场合或测试天气后可以重新生成。
- [x] 手动刷新覆盖当天批次，不新增第二个每日批次。
- [x] AI 失败时仍能看到可解释的模板推荐。
- [x] `npm run check` 通过。

阶段完成记录：

- 完成日期：2026-08-24
- 推荐模型与天气服务：OpenAI Responses API，默认 `gpt-4o-mini`，服务端严格 JSON Schema、`store: false` 和 10 秒 AI 超时；天气使用免密钥 Open-Meteo，北京为默认演示城市，并提供温和、热天、冷天和雨天四组明确标识的模拟天气。
- 验收结果：Supabase 已应用 `daily_recommendations` 迁移并启用四类当前用户所有权 RLS。双匿名会话 `14BF60F7`、`96F1C658` 验证交叉读取与更新被拒绝；两名用户各自同日连续写入 3 次后均只保留 1 个最新批次，数据库拒绝少于 3 套的结果。390px 本地浏览器以 25 件真实演示衣物完成通勤温和天气与约会冷天两轮生成，分别得到恰好 3 套不重复原图卡片；冷天三套均含鞋和外套，修正后不再混用连衣裙与上衣裤装。规则降级实测 11.2～13.3 秒。`npm run check`、`npm run build`、`npm run verify:sdd-005` 和 Vercel 构建全部通过。
- 已知限制：2026-08-23 本轮浏览器验收因本机 Node.js 直连 OpenAI 超时而走规则降级；2026-08-25 已增加 Windows 系统网络栈传输并完成识别 10/10 回归。推荐仍保持 10 秒 AI 时限，模型响应较慢时按设计降级，不阻塞三套结果。Preview 仍受 Vercel Authentication 保护；Supabase Security Advisor 的匿名访问提醒来自当前 P0 匿名体验，策略均以 `auth.uid()` 限制，泄露密码保护继续留到 SDD-002 集中验收。
- 下一步：启动 SDD-006，实现换一件、收藏和偏好反馈；密码设置、退出与重新登录不作为依赖，等用户集中调试。
- Preview：`https://ai-coding-ac3eixa7p-jialin-d583.vercel.app`（READY，部署 `dpl_6JzNeHa6nuTBtjLK1SQbqq6HEKYJ`，受 Vercel Authentication 保护）。
- 提交记录：`223cc79`（SDD-005 规格、数据库、天气与推荐实现、自动验收、Preview 和阶段完成文档）。

### SDD-006：换一件、收藏与偏好反馈

- 状态：已完成
- SDD 目录：`specs/006-outfit-feedback/`
- AI Coding 估算：1 段主对话，复杂度 M
- 依赖：SDD-005
- 目标：让用户能调整不满意的推荐，并让反馈影响后续结果。

功能范围：

- [x] 点击搭配卡中的任意单品后可以“换一件”。
- [x] 从当前衣橱筛出同类、未占用且符合天气/场合的合法候选，并使用稳定本地排序。
- [x] 替换后重新校验该套搭配并更新展示。
- [x] 实现单品和搭配的收藏、取消收藏和收藏列表。
- [x] 实现 3 题冷启动风格和场合偏好问卷。
- [x] 问卷可跳过，跳过时写入默认偏好。
- [x] 对收藏和换件行为加权，更新可解释的风格偏好。
- [x] 记录查看、换件和收藏等反馈事件。

明确不做：

- [x] 不使用穿搭日记作为偏好信号。
- [x] 不实现“不喜欢”功能。
- [x] 不实现协同过滤或模型训练。

独立验收：

- [x] 用户可以替换任意存在合法候选的单品，替换后方案仍通过完整校验。
- [x] 收藏、取消和收藏列表状态正确并持久化。
- [x] 收藏与换入风格会增加对应权重，后续推荐读取新的优先顺序。
- [x] 偏好权重能够说明由哪些问卷或行为产生。
- [x] 所有反馈均按当前用户隔离。
- [x] `npm run check` 通过。

阶段完成记录：

- 完成日期：2026-08-24
- 偏好权重规则：问卷前三风格依次 +3/+2/+1，关注点映射风格 +1；单品收藏/取消 ±1，整套每个风格 ±0.5，换入风格 +0.35，查看为 0。换出单品不记负向偏好；所有分数均由事件账本重算。
- 验收结果：Supabase 已应用收藏、穿搭快照、反馈事件与偏好字段迁移，补齐衣物外键索引，全部新表启用当前用户所有权 RLS。`npm run verify:sdd-006` 以会话 A `C6FE3738`、B `6D309C5D` 验证单品/整套收藏和反馈幂等、跨用户读取与写入被拒绝并自动清理。390px 浏览器完成通勤冷天“沙色阔腿裤 → 靛蓝直筒牛仔裤”合法替换并刷新保持；无候选时保留原方案并说明原因。推荐页完成单品与整套收藏，收藏页显示 1 套和 2 件单品；3 题问卷保存后来源面板显示问卷 4 条、收藏/取消 4 条、换入 1 条，风格排名即时更新。`npm run check`、`npm run build`、Vercel 构建均通过。
- 已知限制：候选仅来自当前衣橱，不推荐外部商品；当三套已占用全部符合条件的同类衣物时会明确显示无候选。当前候选采用确定性本地排序，未额外调用 AI 重排。Preview 继续受 Vercel Authentication 保护；密码设置、退出和重新登录仍保留到与用户集中调试。
- 下一步：启动 SDD-007，完成全链路加固、受控评审文档和最终 Preview；随后与用户一起完成 SDD-002 密码设置与重新登录验收。
- Preview：`https://ai-coding-1ru0zqi0f-jialin-d583.vercel.app`（READY，部署 `dpl_G5M1GZVogSsV8mN48MSiRtVeBuYD`，受 Vercel Authentication 保护）。
- 提交记录：`59454e6`（SDD-006 规格、数据库、换件、收藏、偏好、验收、Preview 与阶段完成文档）。

### SDD-007：全链路加固与受控上线

- 状态：已完成
- SDD 目录：`specs/007-release-deploy/`
- AI Coding 估算：1 段主对话，复杂度 M
- 依赖：SDD-001、SDD-003 至 SDD-006
- 目标：将 P0 发布为可访问、可复现、可在 5 分钟内完成演示的评审版本。

功能范围：

- [x] 完成移动端关键页面布局、交互和视觉一致性检查。
- [x] 覆盖所有核心页面的加载、空数据、错误、超时和重试状态。
- [x] 使用两个匿名会话执行数据库与 Storage 交叉访问验证。
- [x] 以两个全新独立匿名会话验证新身份建立及旧身份数据隔离，不破坏当前已绑定浏览器会话。
- [x] 配置 Vercel 受控评审环境、Supabase URL 和服务端密钥。
- [x] 复核真实 AI 识别证据、服务端推荐配置、天气降级和规则推荐降级；线上真实 AI 推荐保留到集中调试。
- [x] 验证一键加载 20 件以上演示衣橱并完成全链路。
- [x] 完成 `npm run check`、构建检查和核心流程浏览器验收。
- [x] 更新 README、迁移说明、技术方案、演示步骤和已知限制；`supabase/migrations/` 作为唯一建表来源。
- [x] 发布受控评审链接并记录部署版本。

上线验收：

- [x] 匿名用户无需登录即可完成入库、衣橱、推荐、换件和收藏。
- [x] 10 件识别样本结果已记录，原图上传和展示正常。
- [x] 每日 3 套推荐可生成、切换场合、刷新和降级。
- [x] 两个匿名会话的数据及图片互相隔离。
- [x] 完整演示路径按 quickstart 可在 5 分钟内完成。
- [x] 评审链接、环境配置和已知限制已记录。
- [x] `npm run check` 与构建检查通过。

明确不验收：

- [x] 不验收自动抠图。
- [x] 不验收穿搭日记和穿着统计。
- [x] 不把邮箱密码设置、退出/重登录、密码找回、第三方登录和账号切换计入 P0 门禁；SDD-002 继续单独验收。
- [x] 不验收真人虚拟试穿。

阶段完成记录：

- 完成日期：2026-08-24
- 评审链接：`https://ai-coding-84l2zuiur-jialin-d583.vercel.app`（受 Vercel Authentication 保护）
- 部署版本：`dpl_AKheXxJmJbQNPVF1bzybPPbLnp5Z`，READY，Preview，未发布 Production。
- 验收结果：`npm run check`、`npm run build` 和无网络 `npm run verify:sdd-007` 全部通过，静态门禁核对 7 个核心页面、24 张演示图片、10 张识别样本、7 个迁移和服务端密钥边界。复跑 SDD-001、003、005、006 共 8 个新匿名会话，资料、Storage、衣橱、推荐、收藏和反馈的交叉访问均被拒绝并完成清理。390px 七个核心页面 `scrollWidth` 均等于内容视口宽度，共 47 张页面图片无损坏、无运行时错误。Supabase 无缺失 RLS 或未索引外键；Vercel 生产构建 14 个路由并达到 READY。
- 已知限制：Preview 需团队 Vercel 登录；Windows 本地 OpenAI 传输已于 2026-08-25 修复并以当前页面 10 张图片通过真实识别，推荐超过 10 秒仍会按设计规则降级。未自动清除当前浏览器站点数据，因为该会话已绑定邮箱且尚未完成密码设置；以两个全新独立匿名会话完成等价的新身份与旧数据隔离验证。
- 下一步：与用户一起完成 SDD-002 密码设置、退出和重新登录；这是必须由用户输入密码并确认邮箱的人工步骤，继续保持暂停等待，不自动操作。正式 Production、域名与公开访问策略也等待用户明确决定。
- 提交记录：`f798207`（SDD-007 规格、静态发布门禁、README、全链路回归、最终 Preview 与阶段完成文档）。

## 部署检查点

- [x] SDD-001 后：第一个 Preview 可访问，匿名会话、环境变量和基础隔离通过。
- [x] SDD-004 后：识别 Preview 可使用真实 AI 完成原图入库。
- [x] SDD-005 后：核心体验 Preview 已包含“演示衣橱 → 每日推荐”，部署构建为 READY；受保护页面与线上真实 AI 留到集中调试。
- [x] SDD-006 后：反馈体验 Preview 已包含换件、单品/整套收藏和偏好问卷，部署构建为 READY。
- [x] SDD-007 后：受控评审版完成匿名入库、推荐、反馈、发布文档和安全验收；邮箱密码重登录仍按 SDD-002 单独等待人工验收。
- [x] 2026-08-25 Production：新建 `yipai-jihe` 项目，修正框架预设为 Next.js，配置 Production 环境变量并发布 `dpl_Eq4fjWm1ZVY9iETcLPANVEALiWpN`；固定域名 `https://yipai-jihe.vercel.app` 已公开，五个核心页面、匿名会话与带会话衣橱访问均验收通过。

## P1 扩展 SDD

### SDD-002：邮箱绑定与会话恢复

- 状态：验收中（无邮件注册代码与认证配置已完成，等待历史账号本地设密、部署与重新登录）
- SDD 目录：`specs/002-account-binding/`
- AI Coding 估算：1 段主对话，复杂度 M
- 依赖：SDD-001
- 启动条件：已满足。用户已明确要求完成邮箱绑定。
- 目标：让匿名用户可选绑定邮箱，并在不丢失数据的前提下登录恢复。

功能范围：

- [x] 主页为匿名用户提供“注册账号”和“登录”入口。
- [x] 匿名用户可在一个表单填写邮箱和两次密码，将当前身份直接升级为邮箱账号。
- [x] 关闭 Supabase Confirm email；Email、Anonymous Sign-Ins 与 Manual Linking 保持开启。
- [x] 支持邮箱登录、退出和重新登录。
- [x] 绑定后保持原 `auth_user_id`，匿名阶段数据不得迁移到新用户或丢失。
- [x] 设置页展示当前匿名或邮箱账号状态。
- [x] 已绑定邮箱只走密码登录，不再展示或调用密码设置邮件入口。
- [x] 对重复邮箱、弱密码、旧链接失效和登录失败提供明确反馈。
- [x] 邮箱及密码输入框采用独立图标留白，避免文字与图标重叠。

独立验收：

- [ ] 匿名用户可以不打开邮件直接注册、退出并重新登录。
- [x] 绑定前创建的测试数据在账号原地升级后仍属于同一用户。
- [ ] 关闭邮件确认后，重复邮箱或密码错误仍得到明确提示，注册过程不发送验证邮件。
- [x] 其他匿名或邮箱会话无法访问该账号的数据。
- [x] `npm run check` 通过。

明确不做：

- [ ] 不实现第三方登录。
- [ ] 不实现账号切换。
- [ ] 不实现密码找回。

阶段进行记录：

- 实现结果：2026-08-25 已将旧邮件验证流程改为邮箱和密码一次提交的原地注册；主页增加注册/登录入口，登录页移除邮件恢复入口，已绑定账号只走密码登录；新增仅本地可运行的 `account:set-password-local` 管理员工具，未建立线上管理员接口。
- 自动验收：`npm run check`、`npm run build` 通过；`npm run verify:sdd-002` 在网络限制外连接远端 Supabase，验证身份原地更新后用户 ID 与资料保持不变、本地退出清除会话、无效凭据被拒绝，并检查主页入口、无邮件代码边界、本地恢复命令和输入框留白。390px 本地登录页两处输入框左内边距均为 44px，图标与文字起点间距 12px，无横向溢出。
- 安全复核：Supabase Security Advisor 仍只有预期的匿名身份策略提醒和 `Leaked Password Protection Disabled`；所有业务策略继续以当前 `auth.uid()` 或私有路径限制。建议在关闭 Confirm email 时同时开启泄露密码保护，作为无需邮箱确认后的密码安全补偿措施。
- Preview：主体版本 `https://ai-coding-gq6wt4chx-jialin-d583.vercel.app`；密码设置入口版 `https://ai-coding-5sr1vo74n-jialin-d583.vercel.app`（READY）。
- 远端配置：2026-08-25 已经用户明确允许，在 Supabase Dashboard 关闭 Confirm email 并保存；重新加载页面后开关仍为关闭，Email、Anonymous Sign-Ins 与 Manual Linking 保持开启。Site URL 和 Redirect URL 只保留旧链接兼容。本地 `.env.local` 已具备真实 `SECRET_KEY`，该密钥不得进入 Vercel、浏览器或提交。
- Preview 验收：设置页已显示匿名身份、邮箱绑定表单与状态说明，衣橱页正常加载 24 件隔离演示衣物，390px 无横向溢出且控制台无错误。
- 真实账号状态：2026-08-24 已确认匿名编号 `FE462561` 原地升级为邮箱账号，邮箱已验证、用户 ID 未变化，尚未设置登录密码。发现 JWT 旧匿名声明导致页面仍显示绑定表单，已改为使用 Auth 服务端最新用户记录，并让旧表单提交自动转入密码设置页。
- 旧流程记录：曾为跨 Preview 会话增加一次性密码设置邮件入口；2026-08-25 按用户决定已从登录页和服务端动作移除，不再发送恢复邮件。
- 2026-08-24 暂停检查点：新 Preview `https://ai-coding-84l2zuiur-jialin-d583.vercel.app` 使用了另一匿名会话，再次绑定原邮箱时 Supabase Auth 日志明确返回 `email_exists`（HTTP 422）。这证明邮箱仍属于原账号，不应更换邮箱或继续重复绑定。已重新打开原会话域名 `https://ai-coding-gq6wt4chx-jialin-d583.vercel.app`；若该域名仍保留原账号 Cookie，由用户本人直接设置密码；若旧会话已失效，再由用户明确确认是否采用仅限本地的一次性管理员密码重置。管理员方案不得建立线上重置入口，也不得记录密码、邮箱或服务端密钥。
- 2026-08-25 暂停检查点：代码、SDD 文档、自动检查、本地布局验收与 Supabase Confirm email 关闭均已完成。关键待办为：由用户本人在本地终端运行 `npm run account:set-password-local` 为历史账号隐藏输入密码并确认；部署新 Preview 后完成无邮件注册、退出/重登录和原衣橱恢复。不得替用户输入、保存或记录密码，完成真实验收前不得标记 SDD-002 已完成。
- 保存提交：`ee8e5bd`（主页注册/登录入口、无邮件注册流程、输入框修复、本地管理员设密工具、SDD 与验收文档）。

### SDD-011：全局高级动效系统

- 状态：已完成（2026-08-24）
- SDD 目录：`specs/011-global-motion/`
- AI Coding 估算：与 SDD-002 同一段主对话，复杂度 M
- 依赖：SDD-001
- 目标：用统一的空间动效代替全站单一弹跳，并保持苹果式克制质感。

功能范围：

- [x] 建立统一时长、缓动和层级 token。
- [x] 页面进入使用系统蓝/银白气泡扩散与前景分层显现。
- [x] 底部导航使用扩展式选中气泡，移除中央添加按钮上下弹跳。
- [x] 按钮使用填充扩散、边缘光泽和轻微景深反馈。
- [x] 卡片、输入框、状态提示和短列表使用各自语义动效。
- [x] 支持 `prefers-reduced-motion` 与 `prefers-reduced-transparency` 完整降级。
- [x] 不引入新动效依赖，不改变业务路由与数据流。

独立验收：

- [x] `npm run check` 与 `npm run build` 通过。
- [x] Preview 在 390px 和桌面宽度无横向溢出或错误覆盖层。
- [x] 首页、衣橱、添加、推荐、收藏、设置和登录页复用同一全局动效系统，节奏一致。
- [x] 减少动态效果下无持续动画且所有操作可用。

阶段进行记录：

- 实现结果：全局“衣橱气泡”页面动效、导航气泡、按钮光泽/填充、卡片景深、状态显现与无障碍降级均已落地。
- Preview：与 SDD-002 共用 `https://ai-coding-gq6wt4chx-jialin-d583.vercel.app`（READY）。
- 验收结果：桌面与 390px Preview 均正常；390px `scrollWidth=375`、无横向溢出，设置页和衣橱页视觉通过，控制台无错误。页面气泡扩散、内容分层、导航选中气泡、按钮光泽/填充和卡片景深均已生效；键盘焦点和 reduced-motion/reduced-transparency 降级由全局规则覆盖。
- 2026-08-25 回归修复：根据真实使用反馈移除黑色按钮的高对比白色横向扫光；浏览器计算样式确认 `.motion-button::before` 为 `none`，保留径向按压和焦点反馈。
- 提交记录：主体实现见 `52331cc`；Supabase 配置与最终进度记录见本阶段后续提交。

### SDD-012：账号城市与衣着偏好

- 状态：已完成（2026-08-25）
- SDD 目录：`specs/012-personalized-context/`
- AI Coding 估算：1 段主对话，复杂度 M
- 依赖：SDD-003、SDD-005、SDD-011
- 目标：让天气跟随登录账号保存的常用城市，并用可逆衣着偏好统一过滤衣橱与推荐。

功能范围：

- [x] 在账号偏好保存常用城市、行政区、经纬度与时区，位置只精确到城市级。
- [x] 实时天气只使用当前账号城市；未设置时引导设置，不再静默使用北京。
- [x] 增加男装、女装、不限三种衣着偏好，并在设置摘要展示当前值。
- [x] 为衣物增加男装、女装、中性归属；AI 识别、手工表单和详情均可读取或修正。
- [x] 衣橱列表、首页预览和推荐候选复用同一可逆过滤，不删除原衣物。
- [x] 4 件演示连衣裙标记为女装，其余演示衣物保持中性，保证男装模式仍能组成 3 套。
- [x] 改变城市或衣着偏好后使旧当日推荐失效，避免继续展示旧地点或不匹配单品。
- [x] 移除黑色按钮突兀白色扫光，保留克制的径向按压和无障碍降级。

独立验收：

- [x] `npm run check`、`npm run build`、`npm run verify:sdd-012` 全部通过。
- [x] 双会话 A（武汉、男装）与 B（上海、女装）只能读写自身偏好，跨账号访问被 RLS 拒绝。
- [x] 男装过滤后只返回中性测试衣物，数据库原有两件记录仍完整；非法偏好与不完整城市五元组被约束拒绝。
- [x] 当前真实本地账号摘要显示武汉、男装；24 件演示衣橱显示 20 件且不含 4 件裙装。
- [x] 实时推荐显示“武汉，实时天气，晴，体感 38°C”，三套均未使用裙装；OpenAI 超时时在 12.4 秒内使用规则推荐。
- [x] 移动端窄栏应用壳视觉正常，黑色按钮无白条掠过，服务端无运行错误。

阶段完成记录：

- 完成日期：2026-08-25
- 验收结果：数据库迁移已应用到 `gmjtzmxuveoaqcdmuifr`；自动化、生产构建、Supabase 安全/性能顾问和真实浏览器路径均通过。安全顾问没有新增本阶段风险，仍只有已知匿名体验策略和泄露密码保护提醒。
- 已知限制：当前由用户主动保存常用城市，不持续读取设备定位；旧演示数据只把明确连衣裙归为女装，其余保持中性，真实衣物可在入库或编辑时修正归属；本次改动尚未部署到新的 Vercel Preview。
- 下一步：与用户集中完成 SDD-002 历史账号密码与重新登录验收；需要线上查看本阶段时再部署新 Preview，不自动发布 Production。
- 提交记录：`c13ee26`（账号城市、衣着偏好、可逆过滤、武汉天气、按钮回归修复与完整 SDD）。

### SDD-013：今日与明日真实天气搭配

- 状态：已完成（2026-08-25）
- SDD 目录：`specs/013-real-forecast-days/`
- AI Coding 估算：1 段主对话，复杂度 M
- 依赖：SDD-005、SDD-012
- 目标：让用户在今天和明天之间切换，并只用账号城市的真实天气生成对应日期搭配。

功能范围：

- [x] 推荐页增加“今天 / 明天”分段切换，URL、标题、按钮、天气卡和结果文案保持同一目标日。
- [x] 今天读取账号城市当前温度、体感温度和天气代码。
- [x] 明天按账号时区精确匹配 Open-Meteo 日预报日期，读取原始最低温度、最低体感温度和天气代码。
- [x] 今天和明天按不同 `recommendation_date` 分别 upsert，互不覆盖。
- [x] 普通界面移除温和、热天、冷天、雨天等测试天气选择。
- [x] 天气超时、报错、字段缺失或明日日期不匹配时停止生成，不保存模拟天气或新推荐。
- [x] 旧模拟天气批次不再作为当前有效推荐展示。
- [x] AI 失败仍可在真实天气已成功的前提下使用规则搭配。

独立验收：

- [x] `npm run check`、`npm run build`、`npm run verify:sdd-013` 全部通过。
- [x] 独立脚本实际读取武汉两日天气，精确匹配 2026-08-26，得到最低 29°C、最低体感 34°C。
- [x] Supabase 真实匿名会话中今天和明天分别保存为两条记录；同日刷新只覆盖原记录，明日记录不受影响。
- [x] 本地浏览器为武汉生成明日休闲三套，天气卡显示“Open-Meteo 明日预报，晴，最低体感 34°C”，规则推荐在 10.2 秒完成。
- [x] 切回今天后原今日方案和“Open-Meteo 实时天气，晴，体感 38°C”仍存在。
- [x] 390px 宽度下日期切换、生成按钮、天气卡和推荐结果无水平溢出。

阶段完成记录：

- 完成日期：2026-08-25
- 验收结果：规格、计划、数据模型、两个接口契约、16 项任务和独立验收均完成；真实天气、日期隔离、旧模拟结果拒绝、构建与浏览器核心路径全部通过。
- 已知限制：明日穿搭当前以日预报最低体感作为保守厚薄依据；公开天气预报会随气象模型更新，不承诺与未来实况完全一致；本阶段只提供今天和明天，不扩展一周计划；尚未部署新 Vercel Preview。
- 下一步：与用户集中完成 SDD-002 历史账号密码与重新登录验收；需要线上查看本阶段时再部署新 Preview，不自动发布 Production。
- 提交记录：`ca14668`（今日/明日真实天气、日期隔离、禁止模拟天气、完整 SDD 与验收）。

### SDD-014：四场景差异化推荐

- 状态：实现与自动验收已完成，等待 Production 发布
- SDD 目录：`specs/014-scene-diversity/`
- AI Coding 估算：1 段主对话，复杂度 M
- 依赖：SDD-005、SDD-012、SDD-013
- 目标：让通勤、休闲、约会和正式不再只改变标题或配饰，而是在现有衣橱允许范围内体现稳定、可解释的核心单品差异。

功能范围：

- [x] 建立 AI、规则降级和服务端校验共用的四场景画像。
- [x] 每套推荐至少包含两个独立场景信号，避免只靠标题伪装差异。
- [x] 正式场景硬性拒绝仅适用于运动的单品。
- [x] AI 提示加入场景目标、选品方向和雷区，不增加模型调用或提高随机度。
- [x] 规则降级按场景画像排序、分配信号单品并补全完整搭配。
- [x] 天气、当前账号衣橱、衣着偏好、跨套不重复和搭配完整性继续保持高优先级。
- [x] 衣橱有限时优先返回可穿的三套；缺少关键类别时保持明确的衣橱不足提示，不伪造单品。
- [x] 不新增数据库迁移、环境变量、第三方依赖或推荐历史结构。

独立验收：

- [x] `npm run verify:sdd-014` 验证四场景共 12 套、两个场景信号和正式雷区。
- [x] 库存充足的固定样本中，六组场景两两核心单品重合度为 0%～16.7%，均低于 50% 门槛。
- [x] 现有 20 件男装演示衣橱在四场景下均可生成三套且不依赖裙装。
- [x] 热天、冷天、无效 AI 结果、正式运动鞋拒绝、最小可用衣橱和关键鞋类不足边界均通过。
- [x] `npm run check`、`npm run build`、`npm run verify:sdd-005`、`npm run verify:sdd-012` 与 `npm run verify:sdd-013` 全部通过。
- [ ] 发布到既有 `yipai-jihe` Production，并检查固定域名、匿名会话、四场景生成和 error 日志。

阶段完成记录：

- 完成日期：待 Production 验收后填写
- 验收结果：代码与自动验收已通过；`verify:sdd-005` 首次并行运行遇到临时 JWT 时钟误差，单独重跑后双匿名会话、同日覆盖和跨用户 RLS 全部通过；武汉真实明日预报读取与日期隔离通过。
- 已知限制：核心单品重合度目标只在衣橱库存充足时强制；真实小衣橱仍以天气、搭配完整性和不伪造衣物为先，因此不同场景可以合理复用少量基础款。场景差异不保存跨日期历史，也不增加额外 AI 调用。
- 下一步：提交本阶段实现，发布到既有 `yipai-jihe` Production 并完成线上四场景验收；随后继续等待用户集中完成 SDD-002 历史账号密码与重新登录验收。
- 提交记录：待提交后填写。

### SDD-008：自动抠图

- [ ] P0 受控评审版稳定后创建 `specs/008-auto-cutout/`。
- [ ] 通过服务端 `cutoutService` 调用外部抠图 API。
- [ ] 生成透明底图并与单品绑定。
- [ ] 抠图失败保留原图，不阻塞入库和推荐。
- [ ] 增加重试、状态展示、成本和访问权限验收。

### SDD-009：穿搭日记与利用率报告

- [ ] 创建 `specs/009-outfit-diary/`。
- [ ] 支持推荐套装和自定义组合记录。
- [ ] 支持可选上传穿搭原图。
- [ ] 同一用户同一天只保留一条记录。
- [ ] 支持历史回顾、穿着次数和单品利用率报告。
- [ ] 将穿搭记录加入偏好信号。

### SDD-010：留存与分享能力

- [ ] 创建 `specs/010-retention-features/`。
- [ ] 增加穿搭灵感和搭配法则。
- [ ] 增加天气突变提醒。
- [ ] 增加搭配分享图。
- [ ] 根据 P0/P1 数据决定是否继续投入。

## P2 远期范围

- [ ] 为真实虚拟试穿创建独立 SDD，不与 P0/P1 混合。
- [ ] 评估 VITON 系列模型或云 API，复用预留的 `tryonService` 边界。
- [ ] 社区、电商导购、多人衣橱和协同过滤分别评估，不提前建设。

## 尚待确认的外部服务

- [x] SDD-004 已确认 OpenAI Responses API 与默认 `gpt-4o-mini`，并在本地和 Preview 配置 `OPENAI_API_KEY`。
- [x] P0 使用支持图片理解和稳定结构化 JSON 的单一服务，不做多供应商切换。
- [x] SDD-005 已采用免密钥 Open-Meteo；SDD-013 起普通推荐只允许账号城市的真实今日天气或明日预报，测试预设退出用户界面，天气失败直接停止生成。
- [x] 2026-08-25 已新建并使用 Vercel `yipai-jihe` 项目发布 Production；经用户明确同意关闭项目级 SSO，固定 `vercel.app` 域名可公开访问。未设置自定义域名，密码人工验收仍在 SDD-002。
- [ ] SDD-008 开始前确认外部抠图 API；已确定只在 P1 接入。

## 阶段完成后的固定更新模板

完成任一阶段后，在对应阶段的“阶段完成记录”中填写：

```text
- 状态：已完成 / 部分完成 / 阻塞
- 完成日期：YYYY-MM-DD
- 验收结果：通过项、未通过项和实际测试数据
- 已知限制：当前明确未支持的内容
- 下一步：下一阶段或返工事项
- 提交记录：commit hash 和提交说明
```
