# SDD-046 验收

## 自动检查

运行：

```text
npm run check
npm run build
npm run verify:sdd-041
npm run verify:sdd-043
npm run verify:sdd-045
npm run verify:sdd-046
```

046检查覆盖纯白动效描边、静态降级、全路由加载复用、6px白色拼图外框、固定黑色主操作清理、状态语义色与原色夜间对比度。

本地结果：check、build及041/043/045/046全部通过；Next.js 16.3.1生产构建29个页面/API路由成功。原色浅色/夜间加载动画、原色与紫橙夜间首页已用实际样本复验，浏览器无warning/error。

## 视觉检查

1. 在浅色和夜间播放开屏/加载动画，确认衣服到E的色块不变、边缘连续纯白，没有黑边、模糊或光晕。
2. 检查首页衣橱拼图：深紫灰画板外有明显白色厚框，编辑前后颜色与尺寸一致。
3. 切换原色及五套皮肤的夜间模式，检查卡片、输入、导航承托、紫色主操作和状态提示均与纯色背景分层，辅助文字可读。
4. 检查375px、320px及844×390，不应新增横向溢出或加载布局跳动；减少动态时显示静态品牌图标。

实际结果：375px视口内容宽360px、画板宽320px，844×390横屏画板宽440px；两者横向溢出均为0，画板边框均为6px纯白。原色夜间实际读取为背景`#302044`、主文字`#fcf9ff`、主按钮`#8961b3/#ffffff`，卡片和导航边界清晰。

## 边界

本阶段只改视觉语义与等待反馈，不新增业务请求，不改用户数据、供应商配置、密钥或权限。尚未部署Production；线上仍为SDD-045。
