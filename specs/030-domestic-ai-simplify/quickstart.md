# 验收

1. `npm run check`、`npm run build`、`npm run verify:sdd-030`。
2. 390px 隔离体验身份检查五项底栏与三个顶部入口；浅雾丁香渐变、圆润居中标题、语义化 hover/active、键盘焦点和减少动态。黑白紧凑控件可转紫，彩色控件保持原色相，大面积卡片只做轻量叠层；普通底栏 item 必须保持透明，只显示浅紫气泡，中央按钮单独变色。无溢出、控制台错误。
3. 首次身份入口、登录页和登录后 Header 显示 `Ensemble / 衣拍即合`；浏览器标题为双语，`document.fonts.check` 确认本地 Fredoka 已加载，计算字重为 640，320px/390px 不与头像重叠。
4. 个人页保留昵称头像编辑及三项统计，无画布；推荐保留原图、换件、收藏、日记。
5. 旧画布跳转、抠图 410、确认无后台抠图，历史资产不删除。
6. 用户在百炼北京控制台获取 API Key 与 API Host，填入忽略的 `.env.local` 后重启。禁止聊天传密钥。用隔离衣橱验收三套真实 AI 结果及模型来源；未配置必须保持待办。
7. 本阶段不自动部署；发布需单独验收目标项目与凭据。

## 2026-09-06 验收记录

实现提交 `a883763`；浅紫视觉与 Dock 状态修复提交 `892b9c9`；语义交互色修复提交 `0569302`；Ensemble 双语字标提交 `2b0eabe`；圆润粗体 Fredoka 替换提交 `5bb80e6`。check/build、030 固定接口与业务复验、007/014/015/017/024/025/026/028/029、027 离线回归通过。浏览器 320/390/1280px 无溢出，减少动态为 0s；导航可达、旧链接跳转与抠图 410/no-store 已验证，页面运行错误为空。浅紫、语义色和品牌复验中，青柠大卡保持青柠、黑色按钮转克制紫，大面积白色入口保持白色；首次入口及 App Header 均显示 `Ensemble / 衣拍即合`，本地 Fredoka 字体加载成功、计算字重 640，390px 不溢出或与头像重叠。真实千问 Key/Host 未配置，T013 保持未完成，不将模拟结果记为真实模型验收。无 Production 发布。
