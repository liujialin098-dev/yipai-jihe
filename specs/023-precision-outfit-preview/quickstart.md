# 快速验收：精准搭配预览

1. 在项目根目录运行 `npm run check`。
2. 运行 `npm run build`。
3. 运行 `npm run verify:sdd-023`。
4. 启动本地应用，进入推荐页并准备至少一套包含 3～7 件衣物的推荐。
5. 确认默认出现“精准搭配预览”，人物参照和真实衣物原图同时可见。
6. 逐件核对预览图与“衣物实拍核对”清单，确认名称、图片、角色一致。
7. 用 390px 视口检查无横向滚动；点击 AI 效果图按钮前不应产生模型调用提示。
8. 使用无图或图片加载失败样本确认名称占位不影响其他衣物显示。

