# 验收

1. `npm run check`、`npm run build`，以及 `npm run verify:sdd-039`；回归037/038/031/034/035/036/007。
2. 登录本地现有账号，首页有8件就绪透明图时应展示8件。只有1件时只显示1件；没有透明图时保留制作入口，不假造8件、不自动付费抠图。有推荐时拼图仍标为衣橱拼图，推荐文字单独保留。
3. 首页点「描边 · 白色」，选丁香/青柠等色。描边即时换色；同设备刷新和贴纸画板采用该选择。选中同时有勾选与aria-pressed，白色可恢复。
4. 在深色与浅色画板检查衣服外轮廓连续实心，悬停不出现衣物与描边缩放错位。原始照片不冒充透明贴纸；遮罩加载失败保留原图显示。
5. 贴纸画板下载PNG，复核所选描边色；裁切、层级、旋转仍保留。系统分享需在支持文件分享的真机验收。

## 已执行与限制（2026-09-08）

- 发布补充（覆盖下方本地未部署记录）：源提交13d2364已发布Production，部署dpl_7JhasNyXGMafiPNj2X3AHvdnhBcu，正式域名https://yipai-jihe.vercel.app；云端构建14秒、Ready及别名通过。四个无会话页面GET200，error日志无记录；内置浏览器重载超时，登录后照片、下载和分享仍待复验。

- check/build通过；039像素圆盘膨胀/alpha阈值/越界/六色/本地存储与跨标签同步/存储拒绝通过；037实际首页组件0～8件、有无推荐/空数据、038实际入库卡片回归通过。
- 031/034历史阴影与brightness断言按039新合同更新；035/036/007通过。
- 本地固定透明形状样本（`node scripts/verify-sdd-039.mjs --preview`，127.0.0.1:3012）复用实际512px遮罩算法与CSS；390px浅/深背景白色与丁香色、375px及844px横屏无溢出、44px色点、Canvas轮廓生成已验证，无浏览器error。
- 该样本不是账号数据，也不是完整React水合端到端测试。真实照片细毛边、复杂透明原图、实际1080×1350下载和系统分享仍需用户已有贴纸/真机复核；本次没有调用供应商、读写数据库或部署。
- 颜色为设备级外观偏好，不按单件或账号云端保存。最多缓存64个遮罩；源图读取失败不使用模糊阴影兜底。导出与预览共用描边算法，但既有画布布局映射不属于本次重构范围。
