# SDD-003 独立验收：我的衣橱与演示数据

## 前置条件

1. `.env.local` 已配置当前 Supabase 项目的 `NEXT_PUBLIC_SUPABASE_URL` 和 `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`。
2. Supabase 已开启 Anonymous Sign-Ins。
3. `wardrobe_core` 迁移已应用，`wardrobe-images` 保持私有。

## 启动与质量门禁

```powershell
npm run check
npm run build
npm run dev
```

预期：静态检查和构建均成功，本地应用可以创建或恢复匿名会话。

## 核心流程验收

使用 390px 宽的全新浏览器会话：

1. 打开 `/wardrobe`，确认空衣橱提示与“加载演示衣橱”入口不同于筛选无结果提示。
2. 点击加载，确认按钮展示进行中状态，完成后恰好显示 24 件衣物和 6 个类别。
3. 连续再加载 3 次，确认总数仍为 24，已编辑项目不会被还原。
4. 使用名称关键词、类别，以及颜色 + 季节 + 场合组合筛选；确认所有结果均满足条件，清除后恢复 24 件未归档衣物。
5. 打开一件衣物，确认原图、全部基础属性、“未收藏”和“尚未用于推荐”可见。
6. 修改名称及多个属性并保存，确认列表与详情立即更新。
7. 归档该衣物，确认默认列表减少 1 件；切换已归档后可找到并恢复。
8. 再次进入详情并确认永久删除，确认记录消失且直接访问原详情显示未找到。
9. 确认全过程无水平溢出、死链接或无法结束的加载状态。

## 隔离验收

```powershell
npm run verify:sdd-003
```

预期：

- 两个独立匿名会话都能创建、读取、修改和删除自己的测试衣物与私有图片。
- 会话 A 无法读取、修改、删除会话 B 的衣物记录。
- 会话 A 无法下载或删除会话 B 的图片对象，反向检查同样成立。
- 脚本仅使用合成 PNG，完成后清理本次测试记录和图片。

## Supabase 复核

1. 确认 `public.wardrobe_items` 已启用 RLS，四类用户所有权策略存在。
2. 确认 `authenticated` 只有 `SELECT/INSERT/UPDATE/DELETE`，`anon` 无表权限。
3. 确认 `wardrobe-images` 仍为私有，路径第一段为当前 `auth.uid()`。
4. 运行 Security 与 Performance Advisors，记录所有新增提示及处理结果。

## 通过标准

- `spec.md` 中 SC-001 至 SC-007 均有实际证据。
- `tasks.md` 所有任务已勾选。
- `npm run check`、`npm run build`、`npm run verify:sdd-003` 全部通过。
- `progress.md` 与 `AGENTS.md` 已更新完成日期、测试数据、限制、下一步和提交记录。

## 2026-08-21 完成验收结果

- 一次加载得到 24 件，连续补齐 3 次仍为 24；已编辑演示名称不会被补齐操作覆盖。
- 关键词“针织”返回 3 件，上装返回 4 件，黑色 + 冬季 + 通勤返回 3 件；空结果、清除条件和归档筛选正确。真实交互预热后 3 次采样中，关键词和三维组合筛选中位响应分别为 1.707 秒与 1.847 秒。
- 有效编辑、缺少季节的字段错误、归档、归档列表、恢复和取消永久删除均通过。经用户确认，永久删除合成测试衣物“深夜针织上衣·已验收”及对应图片后，原详情显示“没有找到这件衣物”；再次补齐只新增 1 件默认衣物并恢复到 24 件。
- 390px 视口无水平溢出；24 张列表图片无失败，浏览器控制台无错误。
- `npm run check`、`npm run build`、`npm run verify:sdd-003` 通过；棚拍素材增补后的最终隔离会话 A `BE081706`、B `35DFDAD5`。
- 远端表已启用 RLS，4 条用户所有权策略齐全，`authenticated` 仅有 CRUD，`anon` 无 SELECT，`wardrobe-images` 为私有。
- Security Advisors 的 4 条匿名访问提醒符合 P0 匿名体验设计，相关表策略均继续使用 `auth.uid()` 或用户目录限制；泄露密码保护延后到恢复 SDD-002 时处理。
- Performance Advisors 仅提示 `wardrobe_items_seasons_idx` 与 `wardrobe_items_occasions_idx` 尚未产生累计使用记录；二者对应当前数组筛选，不因新建后暂未统计使用而删除。

## 2026-08-21 视觉增补验收

- 依据用户提供的穿搭应用截图，将现有 UI 重构为冷白银灰画布、近黑高对比文字、系统蓝焦点色、统一软圆角和固定底部玻璃 Dock。
- 玻璃材质使用 `backdrop-filter`、半透明层、内边框与内高光实现，属于 Web 端苹果材质近似，不依赖或冒充 Apple 原生组件。
- 保留首页、衣橱、添加、推荐、收藏和设置路由，保留衣物搜索、筛选、详情、编辑、归档、恢复、删除和演示数据操作名称与功能边界。
- 390px 浏览器检查全部已完成路由，无水平溢出、损坏图片或控制台错误；首页、衣橱与详情页完成截图级人工观感复核。

## 2026-08-21 演示素材增补验收

- 24 个 `demo_key` 均映射到 `public/demo-wardrobe/` 下对应的 768px WebP 无人物棚拍图，名称、类别、颜色和材质与目录数据一致。
- 演示图片总大小约 0.63MB；列表、首页最近衣物和详情页共用同一套静态演示素材。
- 只有内置演示衣物使用公开静态图；真实用户衣物继续读取 Supabase 私有 Storage 签名 URL，不改变现有隔离规则。
