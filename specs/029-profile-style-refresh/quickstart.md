# SDD-029 验收

运行 `npm run check`、`npm run build`、`npm run verify:sdd-029`；回归 `npm run verify:sdd-007`、`npm run verify:sdd-015`。

本地使用隔离体验身份打开 `/profile`：

1. 390×844 下检查封面、头像、真实四项统计、最近作品空状态与底部 Dock。
2. 点击“编辑资料”，修改合成昵称并保存，刷新确认；无效昵称必须提示且不保存。
3. 键盘操作展开/收起；设置、衣橱、日记、收藏入口正确。
4. `npm run verify:sdd-029 -- --fixture` 将真实 TSX 组件渲染为 `.next/sdd029-gallery.json`，在隔离浏览器 DOM 临时展示样本，检查长标题与缺图；刷新即恢复，不写入账号数据库。
5. 1440px、系统暗色偏好和减少动态模式验证无溢出、无运行错误。根节点既有 `light` 策略必须保留；自动测试不消耗识别/抠图/图像模型额度。

本阶段不发布 Production。

## 2026-09-06 结果

本轮追加：App 实际 computed backgroundImage 为 `linear-gradient(165deg, rgb(240,234,255) 0%, rgb(248,245,255) 38%, rgb(255,255,255) 82%)`；390px 无溢出、选城说明随面板收起、控制台无运行错误。check/build、015/018/019/028/029 回归通过。专业抠图只调整提示文字与加载文案，没有验证第三方恢复或修改现有图片；未执行付费调用。

- `npm run check`、`npm run build`、SDD-007/015/029 均通过；029 除静态边界还直接渲染实际预览组件，覆盖标题可选、旋转、优先透明图、原图和缺图回退。
- 新建隔离体验身份，0 项真实统计与空作品状态正确。空白昵称提示 1～20 字限制；合成昵称“林的穿搭测试”保存后刷新仍显示，编辑区收起。
- 390×844、1440×1000 的文档宽度等于视口；键盘 Enter 可切换原生 details；减少动态下 `.page-enter` 的 animationName 为 none；浏览器运行错误为空。
- 系统暗色偏好下保留项目固定浅色，未新增夜间模式。作品视觉使用离线真实组件样本，不宣称做了新的数据库保存/分享/抠图验收。头像上传、RLS、分享契约未修改，本次未重复付费调用或远端隔离回归。
