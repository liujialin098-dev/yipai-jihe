# 验收

1. `npm run check`、`npm run build`、`npm run verify:sdd-038`，回归033与037。
2. 本地服务端需要有效BAIDU_API_KEY和BAIDU_SECRET_KEY；禁止把值放进测试脚本、输出或浏览器。未配置时应观察失败保留原图，不得声称已就绪。
3. 添加一张衣物照片，识别后核对或手工填写，确认这件；先出现已入库，再显示制作状态；成功后白边透明图及去贴纸画板入口可见。
4. 批量10件逐件处理，某件失败不阻塞下一件，不影响入库；重复按确认不重复请求；全部入库但仍在制贴时不能清空队列。
5. 等待期间离开再回来，衣橱记录仍在；贴纸页可显式继续未完成项。当前实现不承诺跨页持续队列。

2026-09-08：固定队列和实际confirmItem/卡片渲染样本通过；本地两项百度凭据缺失，真实供应商验收待补。SDD-018回归被历史城市说明文案断言阻断，与本次文件无关，未删除断言或扩大修改天气功能。
