# 验收

1. `npm run check`、`npm run build`、`npm run verify:sdd-030`。
2. 390px 隔离体验身份检查五项底栏与三个顶部入口；紫色渐变、圆润居中标题、紫色 hover/active、键盘焦点和减少动态。无溢出、控制台错误。
3. 个人页保留昵称头像编辑及三项统计，无画布；推荐保留原图、换件、收藏、日记。
4. 旧画布跳转、抠图 410、确认无后台抠图，历史资产不删除。
5. 用户在百炼北京控制台获取 API Key 与 API Host，填入忽略的 `.env.local` 后重启。禁止聊天传密钥。用隔离衣橱验收三套真实 AI 结果及模型来源；未配置必须保持待办。
6. 本阶段不自动部署；发布需单独验收目标项目与凭据。

## 2026-09-06 验收记录

实现提交 `a883763`。check/build、030 固定接口与业务复验、007/014/015/017/024/025/026/028/029、027 离线回归通过。浏览器 320/390/1280px 无溢出，56px 添加按钮 hover 紫色，减少动态为 0s；导航可达、合成昵称保存成功、旧链接跳转与抠图 410/no-store 已验证，页面运行错误为空。真实千问 Key/Host 未配置，T013 保持未完成，不将模拟结果记为真实模型验收。无 Production 发布。
