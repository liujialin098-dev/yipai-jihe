# 计划与边界

1. 首页拼图改为当前可见衣橱就绪集合（homePreviewItems已限制8）；不改变推荐items或数据读取。
2. lib/stickers/outline.ts：有界尺寸透明蒙版，alpha阈值后圆形核膨胀、纯色填充。预览遮罩与导出共用；不使用阴影/放大替代轮廓。
3. 客户端GarmentSticker仅在已有透明URL时计算遮罩，保留图片alt；useSyncExternalStore同步当前设备的纯色偏好，存储失败仍可临时切换。不新增网络数据写入。
4. 首页折叠色点与画板色点复用，44px点击区与勾选/名称，不仅靠颜色传达选中。保持当前字体和浅紫/青柠视觉。
5. 固定像素、真实组件和浏览器样本；记录算法与既有导出布局差异，不宣称真实新抠图验收。
